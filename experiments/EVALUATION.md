# 評価まとめ（こう評価した）

PVトリアージ支援パイプラインを、**安全加重（過小コール最優先）**の視点で評価した記録。
数値を誇張せず、限界と「示せたこと／示せていないこと」を明確に分ける方針で書いている。

- **評価対象**: 取り込み → 6軸トリアージ（自社品・抽出・MedDRA・重篤度・因果・既知未知）→ 過去判例(precedent) → 過去データ反映(influence) → HITL。
- **標本**: 合成ICSR 7症例・31事象（`data/sample_reports/`、`data/gold/`）。難症例2件（婉曲表現の重篤+未知／抽出精度）を含む。
- **ゴールド**: Claude起案・**要PVレビュー**（独立ゴールドは未実施＝下記E4）。
- **再現**: `experiments/scripts/*.py` を project venv で実行。temperature=0でもLLMは完全決定的でないため単一実行スナップショット。

## 実験と主結果

| 実験 | 何を見るか | 主結果 |
|---|---|---|
| **精度＋A/B** [triage_eval.md](triage_eval.md) | 対ゴールドの軸別一致・過小/過大、influence反映/参考の差 | MedDRA **100%**・因果 **100%**・既知未知 **100%**・重篤度 94–97%。**過小コール0**（全軸）。A/Bは1件のみ安全側↑・**格下げ0** |
| **E1 ロバスト性** [triage_robustness.md](triage_robustness.md) | K=3回実行の判定安定性・精度ばらつき・安全安定性 | 判定の全一致率 重篤度97%/因果100%/既知未知100%/PT100%。重篤度一致 94%（92–96）。**過小コールは全runで0** |
| **E2 ベースライン対比** [triage_baseline.md](triage_baseline.md) | 単一LLM1回 vs 多段パイプライン | MedDRA **32%→100%**、既知/未知 **43%→100%**（ベースラインは既知/未知で**過小1**）。重篤度は逆に100%→94%（多段が安全側に過大）、因果・recallは同等 |
| **E3 ホールドアウト** [triage_holdout.md](triage_holdout.md) | LOOで未知症例に学習(precedent)が効くか/安全か | 未知症例で precedent の上書き **0件**・格下げ0・**新規過小0**。学習の精度向上は**実証されず** |

## 示せたこと（頑健）
1. **安全側インバリアントが頑健に成立**：過小コール0が、本評価・K=3全run・ホールドアウトの**すべて**で維持。influenceの**格下げ0**も全所で成立。PVトリアージの最重要要件（見落とさない）を満たす。
2. **多段＋RAG＋辞書の価値が定量化**：単一LLMに対し MedDRA +68pt（32→100）、既知/未知 +57pt（43→100）。既知/未知ではベースラインが安全側の過小を1件出したのに対しパイプラインは0＝**RAGが安全性にも寄与**。
3. **判定の再現性が高い**：run間の全一致率 ≥97%。抽出はナラティブ由来事象で軽微に揺れる（正直な弱点）。

## 示せていないこと（正直に）
1. **「フィードバックで精度が上がる」は未実証**。ホールドアウトでは未知症例に対し precedent は何も上書きせず（0件）。素判定の過小が既に~0で「直すべき誤り」が乏しいため。本評価(seed入り)で見えた1件の安全側変化は**seed依存のアーティファクト**で、ホールドアウトがそれを暴いた。→ 学習効果は「素判定が誤る難症例をより多く含む集合」でこそ検証すべき。
2. **重篤度・因果は単一LLMが競合**。多段は重篤度で安全側に過大（94% vs 100%）。過大は過小より許容だが、コスト（アラート疲れ）は別途評価が要る。
3. **標本が小規模・gold自作＝循環**。7症例31事象は高分散、100%は「誤りなし」であって精度100%ではない。ゴールドはシステムの保守的思考で付けており、一致は「作者との一致」を含む。天井（アノテータ間一致）は未測定。

## 総合評価（私の見立て）
> **小規模・自作ゴールド上の回帰ベースラインとして、(1)安全側インバリアント（過小0・格下げ0）が本評価・反復・ホールドアウトで頑健に成立し、(2)多段＋RAG＋辞書がMedDRA/既知未知で単一LLMを大きく上回ることを定量化した。** 一方で「学習が精度を上げる」は**この規模・一貫性では実証できず**、seed依存の見かけの効果をホールドアウトが正しく否定した。エビデンスとしては入口だが、**測定の作法と安全性の裏づけ**は形になっている。

## 完全な検証への道筋（次段）
- **E4 独立/専門家ゴールド**：PV専門家が（できればブラインドで）正解を確定し、アノテータ間一致で天井を測る。現状の循環を解く最重要ピース。
- **規模拡大**：素判定が誤る難症例を増やし、ホールドアウトで学習効果に「信号」を出す。
- **N回×多数**：点推定→CI。
- **過大コストの評価**：安全側過大の実務コスト（余分な expedited 報告・レビュー負荷）を定量化。
