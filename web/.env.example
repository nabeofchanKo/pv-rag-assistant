# FastAPI backend base URL, used server-side by the BFF route handlers
# (src/app/api/*). The browser never uses this. Defaults to http://localhost:8000
# when unset. In production (Amplify), point this at the App Runner URL.
BACKEND_URL=http://localhost:8000
